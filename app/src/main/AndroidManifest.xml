<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asantherrera.savemoney365days">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_savemoney"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_savemoney_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaveMoney365days">
        <activity android:name=".ui.about.InfoActivity"></activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/SplashTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>